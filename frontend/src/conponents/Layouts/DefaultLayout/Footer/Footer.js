import clsx from 'clsx';
import style from './footer.module.css';

const Footer = () => {
    return (
        <div className={clsx(style.container, 'footer')} >
            <h1 >FOOTER</h1>
        </div>
    )
};

export default Footer;
